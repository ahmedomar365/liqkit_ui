name: liqkit_ui_tokens
description: Internal scaffold for liqkit_ui's token regenerator. The published surface lives inside `package:liqkit_ui` (`lib/src/tokens/_generated/`). Not published.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.7.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
