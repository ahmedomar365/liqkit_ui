/// Lucide-backed icon set for liqkit_ui.
///
/// Re-exports a curated subset of Lucide icons under the `LiqIcons`
/// namespace, so consumers can write `Icon(LiqIcons.search)` inside any
/// `liqkit_ui` slot that expects an `Icon` widget.
///
/// Lucide icons are licensed under ISC. See:
/// https://github.com/lucide-icons/lucide
library;

export 'src/liq_icons.dart' show LiqIcons;
