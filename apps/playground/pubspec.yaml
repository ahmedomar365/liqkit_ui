name: playground
description: Dev-only sandbox for ad-hoc liqkit_ui experiments.
publish_to: none
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.7.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  liqkit_ui:
    path: ../../packages/liqkit_ui

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
