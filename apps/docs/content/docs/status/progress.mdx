---
title: Progress
description: iOS 26 progress indicators — determinate linear bar and indeterminate spinner.
---

import progressLinearSnippet from '@/snippets/progress/linear.json';
import progressSpinnerSnippet from '@/snippets/progress/spinner.json';

Two widgets cover the iOS 26 progress indicator patterns:

- **`LiqProgressBar`** — a 4pt tall determinate horizontal track filled with
  an accent-blue fill. Pass `value` in `[0, 1]` to set the fill amount.
- **`LiqSpinner`** — an indeterminate 30pt rotating ring (900ms linear loop).
  Use `size: LiqSpinnerSize.small` for the 22pt compact variant.

## Linear

A horizontal bar that communicates determinate progress. Set `value` between
`0.0` and `1.0` to fill the track proportionally.

<LiqPreview component="progress" variant="linear" />

<CodeSnippet snippet={progressLinearSnippet} />

## Spinner

An indeterminate spinner for operations with unknown duration. Animates
continuously as a 270-degree arc rotating over a faint background arc.

<LiqPreview component="progress" variant="spinner" />

<CodeSnippet snippet={progressSpinnerSnippet} />
