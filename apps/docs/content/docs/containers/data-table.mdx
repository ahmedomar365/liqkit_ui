---
title: Data Table
description: Simple tabular display with column headers, body rows, optional row taps, and optional sortable columns.
---

> **Cross-platform extension.** iOS 26 surfaces tabular data as grouped lists (`LiqList` / Settings-style sections),
> not as classic header-plus-body tables — the system has no native equivalent. The closest references are
> [shadcn/ui](https://ui.shadcn.com) and [forui.dev](https://forui.dev). liqkit_ui ships this rendered in iOS 26
> visual language (Liquid Glass surfaces, San Francisco type, iOS color palette) so it composes cleanly with the
> rest of the library, but the interaction model itself is web-native rather than iOS-canonical.

import dataTableDefaultSnippet from '@/snippets/data-table/default.json';
import dataTableSortableSnippet from '@/snippets/data-table/sortable.json';

`LiqDataTable` is the iOS 26 simple data table — a header row plus body
rows with hairline separators inside a rounded card. Width is provided
by the parent constraints; columns share that width using the `flex`
factor on each `LiqDataColumn`.

The table is intentionally not a full-blown spreadsheet. It does not
sort, paginate, or virtualize the data on its own — the consumer
reorders the `rows` list in response to `onSortChanged` and passes the
updated list back in. Sort cycle is `none → ascending → descending →
none`.

## Default

A static three-column table. `numeric: true` right-aligns the column
and renders cell text with tabular numerals so digits line up.

<LiqPreview component="data-table" variant="default" />

<CodeSnippet snippet={dataTableDefaultSnippet} />

## Sortable

Mark a column with `sortable: true` to make its header tappable. The
table fires `onSortChanged(columnIndex, nextDirection)` on each tap;
the consumer is responsible for reordering the `rows` array and
passing back the new `sortColumnIndex` + `sortDirection`. Tapping the
same column three times cycles back to `none`.

<LiqPreview component="data-table" variant="sortable" />

<CodeSnippet snippet={dataTableSortableSnippet} />
