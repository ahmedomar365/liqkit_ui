---
title: Glass Surface
description: The canonical iOS 26 Liquid Glass material — every glass-bearing component in liqkit_ui consumes this primitive.
---

import glassSurfaceFloatingSnippet from '@/snippets/glass-surface/floating.json';
import glassSurfaceFlatSnippet from '@/snippets/glass-surface/flat.json';
import glassSurfaceDarkSnippet from '@/snippets/glass-surface/dark.json';

`LiqGlassSurface` is the canonical iOS 26 Liquid Glass material — the
single primitive every glass-bearing component in liqkit_ui consumes
to draw a translucent panel. It paints, in order, a backdrop blur, a
tint-specific uniform material fill, and a 0.5pt hairline rim. Sheets, popovers, dialogs,
toolbars, action sheets, drawers, and hover cards all reduce to a
single call to this widget — instead of hand-rolling colors and
shadows.

The widget exposes two enums as the public knobs: `LiqGlassTint`
(`light`, `dark`, `opaque`) drives the base fill and rim, and
`LiqGlassElevation` (`flat`, `floating`, `modal`) drives the outer
drop shadow.

## Floating

The default. A light translucent panel over arbitrary content with a
soft drop shadow — used by dialogs, popovers, hover cards, and
drawers.

<LiqPreview component="glass-surface" variant="floating" />

<CodeSnippet snippet={glassSurfaceFloatingSnippet} />

## Flat

`LiqGlassElevation.flat` removes the outer shadow. Use it for
inline panels — sidebars, grouped settings rows — that should appear
as part of their parent rather than floating above it.

<LiqPreview component="glass-surface" variant="flat" />

<CodeSnippet snippet={glassSurfaceFlatSnippet} />

## Dark

`LiqGlassTint.dark` switches to the iOS 26 dark glass palette: an
opaque-enough dark material fill with an 8% white inner rim. Use this for
dark-mode action sheets, the dark status bar, and any glass surface
overlaid on dark content.

<LiqPreview component="glass-surface" variant="dark" />

<CodeSnippet snippet={glassSurfaceDarkSnippet} />
