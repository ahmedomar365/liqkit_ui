---
title: Materials
description: iOS 26 material specimens — live translucent surfaces in light and dark variants.
---

import materialsLightSnippet from '@/snippets/materials/light.json';
import materialsDarkSnippet from '@/snippets/materials/dark.json';

`LiqMaterialChip` renders a live translucent material surface. It uses
`BackdropFilter` to sample whatever sits behind it, then applies a
single brightness-appropriate tint, hairline rim, and soft shadow. The
examples below place real liqkit material over a continuous content
backdrop so the glass effect is visible without baked screenshots.

Five thickness presets (`ultraThin` → `chrome`) control overlay alpha
and blur radius; the examples below use the default `regular` preset.

## Light

The light variant renders a white-tinted surface that lets the content
behind it softly show through.

<LiqPreview component="materials" variant="light" />

<CodeSnippet snippet={materialsLightSnippet} />

## Dark

The dark variant renders one unified dark material surface with the same
live backdrop sampling, not a separate top and bottom treatment.

<LiqPreview component="materials" variant="dark" />

<CodeSnippet snippet={materialsDarkSnippet} />
