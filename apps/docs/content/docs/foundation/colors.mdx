---
title: Colors
description: 40 canonical iOS 26 color tokens, sourced from the Figma variable-defs.
---

import colorsSwatchGridSnippet from '@/snippets/colors/swatch-grid.json';

`liqkit_ui` ships the full iOS 26 colour palette as typed Dart
constants — same names, same values as Apple's HIG. Every token is
available in a default and high-contrast mode via `LiqColorMode`.

## Swatch grid

Every canonical token at a glance.

<LiqPreview component="colors" variant="swatch-grid" />

<CodeSnippet snippet={colorsSwatchGridSnippet} />
