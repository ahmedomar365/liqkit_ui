---
title: Radio
description: iOS 26 single-select radio control, available as a free-standing widget or a vertical group with labels.
---

> **Cross-platform extension.** iOS 26 patterns this differently — single-select pickers in Settings and Mail use
> a checkmark on the active list row (see `LiqList`), not a classic radio circle. This component ships the more
> familiar [shadcn/ui](https://ui.shadcn.com) / [forui.dev](https://forui.dev) circle radio for cross-library
> parity, dressed in iOS 26 visual language (Liquid Glass surfaces, San Francisco type, iOS color palette) so it
> composes cleanly with the rest of the library.

import radioSingleSnippet from '@/snippets/radio/single.json';
import radioGroupSnippet from '@/snippets/radio/group.json';

`LiqRadio` is the iOS 26 radio circle. It is a 22pt ring that animates
in a centered 12pt filled dot when its `value` matches the surrounding
`groupValue`. The visible ring sits inside a 44pt tap target for
comfortable hit testing and renders at 40% opacity when disabled.

## Single

Three free-standing `LiqRadio` widgets sharing a single selection
state. Tap any one to make it the active value.

<LiqPreview component="radio" variant="single" />

<CodeSnippet snippet={radioSingleSnippet} />

## Group

`LiqRadioGroup` lays out a vertical list of `(value, label)` pairs as
tappable rows. Tapping anywhere on a row selects that row's value.

<LiqPreview component="radio" variant="group" />

<CodeSnippet snippet={radioGroupSnippet} />
