include: ../../analysis_options.yaml

# The two files below pre-date the workspace-ification of
# tooling/gen and have a 18-issue lint backlog. Excluded for now;
# cleanup belongs in a separate PR. The scripts continue to run
# cleanly via `dart run`.
analyzer:
  exclude:
    - capture_canonical_tokens.dart
    - generate_canonical_dart.dart
